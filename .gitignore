node_modules/
.mintlify/
.next/
.DS_Store

# Terraform doc sync temp files (script cleans up, but just in case)
/tmp/tf-docs-sync-*

# Sentry docs sparse checkout (refresh with git -C sentry-docs pull)
/sentry-docs/
